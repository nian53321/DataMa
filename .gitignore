# ===================== 环境与密钥（绝不提交）=====================
.env
*.env
!back/.env.example
!front/.env.development
!front/.env.production
master.key
*.key
*.pem
密钥.txt

# ===================== 依赖与编译产物 =====================
node_modules/
dist/
dist-ssr/
build/
*.local

# ===================== Python 缓存 =====================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# ===================== 日志与本地数据 =====================
back/logs/
*.log
data_lake/
migrations/

# ===================== 误产生的嵌套项目副本 =====================
# git clone 到项目内部产生的副本目录（含 .env / keys 等敏感文件，不入库）
git/

# ===================== IDE 与编辑器 =====================
.idea/
.vscode/
.trae/
front/front.iml

# ===================== 操作系统 =====================
.DS_Store
Thumbs.db
desktop.ini

# ===================== 非部署必需的资料（样例数据/截图/测算文档）=====================
样例/
样例2/
manual_screenshots/
测算申请/
.trae/documents/

# ===================== 非源代码文档与一次性脚本 =====================
*.docx
多模态数据标注与分析处理平台用户手册.docx
capture_screenshots.py
capture_screenshots_v2.py
generate_word_manual.py
改动需求.md
*.tmp.py
.usb_check_tmp.py

# ===================== 后端脚本（仅保留部署必需的子进程）=====================
# back/scripts/ 是部署必要目录（Dockerfile COPY . . 会拷入镜像）：
# 容器内摄像头子进程 orbbec_camera_proc.py / realsense_child.py 必须纳入版本管理；
# 其余一次性诊断/调试脚本不纳入。
back/scripts/*
!back/scripts/orbbec_camera_proc.py
!back/scripts/realsense_child.py
back/tests/

# ===================== 深度摄像头运行数据与旧架构 =====================
# 录制输出（mkv/zip 体积巨大）与 OrbbecSDK 日志为运行时产物，不入库
output/
Log/
capture/
# 宿主机采集服务（旧架构降级路径，已被容器内 pyk4a 直连取代，含数百 MB 第三方二进制）
camera_service/
