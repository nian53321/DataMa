# ===================== 环境与密钥（绝不提交）=====================
.env
*.env
!back/.env.example
!front/.env.development
!front/.env.production
master.key
*.key
*.pem
密钥.txt
# 宿主机 USB 透传代理配置（含访问令牌，由 install_usb_agent.ps1 生成）
usb_agent_config.json

# ===================== 依赖与编译产物 =====================
node_modules/
dist/
dist-ssr/
build/
*.local

# ===================== Python 缓存 =====================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# ===================== 日志与本地数据 =====================
back/logs/
*.log
data_lake/
migrations/

# ===================== 本地临时目录（QA 虚拟环境 qa-venv 等，不入库）=====================
tmp/

# ===================== 误产生的嵌套项目副本 =====================
# git clone 到项目内部产生的副本目录（含 .env / keys 等敏感文件，不入库）
git/

# ===================== IDE 与编辑器 =====================
.idea/
.vscode/
.trae/
front/front.iml

# ===================== 操作系统 =====================
.DS_Store
Thumbs.db
desktop.ini

# ===================== 非部署必需的资料（样例数据/截图/测算文档）=====================
样例/
样例2/
样例3/
样例4/
样例5/
量表/
manual_screenshots/
测算申请/
.trae/documents/

# ===================== IDE 内部工作目录（AI 生成物/会话记忆，不入库）=====================
.trae-html-share-packages/
.workbuddy/

# ===================== 一次性提案/软著材料（非部署必需，不入库）=====================
ai-agent-proposal/
软著材料/

# ===================== 非源代码文档与一次性脚本 =====================
*.docx
*.pdf
README.pdf
多模态数据标注与分析处理平台用户手册.docx
capture_screenshots.py
capture_screenshots_v2.py
generate_word_manual.py
改动需求.md
*.tmp.py
.usb_check_tmp.py

# ===================== 人脸检测模型（缺失时由代码自官方自动下载，不入库）=====================
back/models/

# ===================== 脱敏基准实验产物（开发期测算数据，不入库）=====================
# bench_video_desens / bench_audio_desens 内含 mp4/jpg/大量实验 json，运行时不需要。
back/tools/bench_video_desens/
back/tools/bench_audio_desens/

# ===================== back/tools 下的开发文档与一次性诊断脚本（不入库）=====================
# back/tools/ 唯一需要版本管理的是交付物本身（restore_signal_desens.py）；
# README-*.md 是开发期分析结论（同类内容已落在 .workbuddy/memory/ 的分册里），
# _diag_*.py 是一次性诊断脚本（如深度码流体积扫描、DMEC 单文件解密）。
back/tools/README-*.md
back/tools/_diag_*.py

# ===================== 后端脚本（仅保留部署必需的子进程）=====================
# back/scripts/ 是部署必要目录（Dockerfile COPY . . 会拷入镜像）：
# 容器内摄像头子进程 orbbec_camera_proc.py / realsense_child.py 必须纳入版本管理；
# 其余一次性诊断/调试脚本不纳入。
back/scripts/*
!back/scripts/orbbec_camera_proc.py
!back/scripts/realsense_child.py
back/tests/

# ===================== 深度摄像头运行数据与旧架构 =====================
# 录制输出（mkv/zip 体积巨大）与 OrbbecSDK 日志为运行时产物，不入库
output/
Log/
capture/
# 宿主机采集服务（旧架构降级路径，已被容器内 pyk4a 直连取代，含数百 MB 第三方二进制）
camera_service/
